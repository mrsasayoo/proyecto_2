# Fase 0 — Fixes v2 (DenseNet3D + Correcciones de auditoría)

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Aplicar las correcciones identificadas en la auditoría completa de Fase 0: reemplazar referencia CvT-13 por DenseNet3D, añadir verificación de SimpleITK, renombrar `split_isic`, estratificar splits LUNA, eliminar duplicación de Páncreas en CAE CSV, y añadir guardia pre-CAE en `run_splits`.

**Architecture:** Dos archivos independientes modificados en paralelo. `fase0_pipeline.py` recibe 2 cambios (paso7 + paso0). `pre_modelo.py` recibe 5 cambios (rename + estratificación + dedup + guardia + documentación umbral).

**Tech Stack:** Python 3.11+, NumPy, pandas, scikit-learn (train_test_split, StratifiedShuffleSplit)

---

## Grupo A — `src/pipeline/fase0/fase0_pipeline.py`

### Tarea A1: Reemplazar paso7_cvt13 por paso7_densenet3d

**Archivos:**
- Modificar: `src/pipeline/fase0/fase0_pipeline.py` (líneas 555–620 y referencias en run_fase0)

**Contexto:** El Paso 7 verificaba la presencia de `backbone_cvt13.py`. El modelo real del proyecto es DenseNet3D (11.14M parámetros, entrada `[B,1,64,64,64]`, salida `[B,2]`). El archivo de arquitectura relevante es `src/pipeline/fase1/backbone_densenet3d.py` (o el módulo del checkpoint en `checkpoints/expert_03_vivit_tiny/`).

**Cambios exactos:**

1. Reemplazar la constante:
```python
# ANTES (línea 558):
CVT13_MODULE_PATH = SCRIPTS_DIR / "cvt13_backbone.py"

# DESPUÉS:
DENSENET3D_MODULE_PATH = PROJECT_ROOT / "src" / "pipeline" / "fase1" / "backbone_densenet3d.py"
```

2. Reemplazar la función completa `paso7_cvt13`:
```python
def paso7_densenet3d(dry_run=False):
    # type: (bool) -> dict
    """
    Verifica que el backbone DenseNet3D esté disponible para Fase 1.
    Arquitectura: DenseNet-121 3D — 11.14M params, entrada [B,1,64,64,64], salida [B,2].
    Ref: luna16-online-aug.ipynb — growth_rate=32, block_config=(6,12,24,16).
    """
    log.info("╔══ Paso 7: DenseNet3D backbone ══╗")

    # 7a: Verificar módulo nativo en Fase 1
    if DENSENET3D_MODULE_PATH.exists():
        log.info(
            "  ✓ backbone_densenet3d.py encontrado en src/pipeline/fase1/ (nativo)"
        )
        # 7b: Sanity check — importar DenseNet3D y verificar forward pass
        if not dry_run:
            try:
                import importlib.util
                import sys as _sys

                spec = importlib.util.spec_from_file_location(
                    "backbone_densenet3d", str(DENSENET3D_MODULE_PATH)
                )
                mod = importlib.util.module_from_spec(spec)
                spec.loader.exec_module(mod)  # type: ignore[union-attr]
                DenseNet3D = mod.DenseNet3D  # noqa: N806
                import torch

                model = DenseNet3D(
                    growth_rate=32,
                    block_config=(6, 12, 24, 16),
                    num_init_features=32,
                    bn_size=4,
                    dropout_rate=0.2,
                    fc_dropout=0.4,
                    num_classes=2,
                    compression_rate=0.5,
                )
                dummy = torch.zeros(1, 1, 64, 64, 64)
                with torch.no_grad():
                    out = model(dummy)
                assert out.shape == (1, 2), "Shape esperado (1,2), got {}".format(out.shape)
                n_params = sum(p.numel() for p in model.parameters() if p.requires_grad)
                log.info(
                    "  ✓ DenseNet3D forward pass OK — %s params entrenables",
                    "{:,.0f}".format(n_params),
                )
                return {"status": "✅", "n_params": n_params}
            except ImportError:
                log.info(
                    "  ⚠ torch no instalado — verificación forward pass omitida"
                )
                return {"status": "✅", "note": "torch ausente, skip sanity"}
            except Exception as exc:
                log.error("  ✗ DenseNet3D sanity check falló: %s", exc)
                return {"status": "❌", "error": str(exc)}
        return {"status": "✅", "dry_run": True}

    # 7c: Verificar checkpoint preentrenado como fallback
    ckpt_path = (
        PROJECT_ROOT
        / "checkpoints"
        / "expert_03_vivit_tiny"
        / "expert3_best.pt"
    )
    if ckpt_path.exists():
        log.info(
            "  ⚠ backbone_densenet3d.py no encontrado pero checkpoint existe: %s",
            ckpt_path,
        )
        log.info("  → Ejecutar refactorización de Fase 1 para generar backbone_densenet3d.py")
        return {"status": "⚠️", "reason": "backbone_densenet3d.py ausente, checkpoint disponible"}

    log.warning(
        "  ✗ backbone_densenet3d.py no encontrado en src/pipeline/fase1/. "
        "Fase 1 no podrá cargar el expert LUNA."
    )
    return {"status": "⚠️", "reason": "backbone_densenet3d.py no disponible"}
```

3. En la función `run_fase0` (o equivalente que llame a paso7), reemplazar la llamada:
```python
# ANTES:
results["paso7"] = run_paso("paso7", paso7_cvt13, dry_run=dry_run)

# DESPUÉS:
results["paso7"] = run_paso("paso7", paso7_densenet3d, dry_run=dry_run)
```

**Verificación:**
```bash
python -c "import ast; ast.parse(open('src/pipeline/fase0/fase0_pipeline.py').read()); print('OK')"
```

---

### Tarea A2: Añadir verificación SimpleITK en paso0_prerequisites

**Archivos:**
- Modificar: `src/pipeline/fase0/fase0_pipeline.py` (líneas 225–288)

**Contexto:** SimpleITK es necesario para el Paso 6 (extracción de parches 3D de LUNA y Páncreas). Sin él, el pipeline falla silenciosamente en Paso 6 sin diagnóstico útil en Paso 0.

**Cambio exacto — insertar ANTES de "Espacio en disco" (línea ~269):**
```python
    # SimpleITK — necesario para Paso 6 (extracción de parches 3D)
    if any(d in active for d in {"luna_ct", "pancreas"}):
        try:
            import SimpleITK  # noqa: F401
            log.info("  ✓ SimpleITK disponible")
        except ImportError:
            msg = (
                "SimpleITK no instalado — Paso 6 fallará. "
                "Instalar: pip install SimpleITK"
            )
            log.error("  ✗ %s", msg)
            issues.append(("BLOQUEANTE", msg))
```

**Verificación:**
```bash
python -c "import ast; ast.parse(open('src/pipeline/fase0/fase0_pipeline.py').read()); print('OK')"
```

---

## Grupo B — `src/pipeline/fase0/pre_modelo.py`

### Tarea B1: Renombrar split_isic → build_lesion_split

**Archivos:**
- Modificar: `src/pipeline/fase0/pre_modelo.py` (línea 202 y línea 1108)

**Contexto:** La arquitectura §7.4 referencia consistentemente `build_lesion_split` como el método de split de ISIC. El código usa `split_isic()`. Inconsistencia de nomenclatura.

**Cambios exactos:**

```python
# ANTES (línea 202):
def split_isic(datasets_dir):

# DESPUÉS:
def build_lesion_split(datasets_dir):
```

```python
# ANTES (línea 1108 aprox, dentro de run_splits):
            results["isic"] = split_isic(datasets_dir)

# DESPUÉS:
            results["isic"] = build_lesion_split(datasets_dir)
```

---

### Tarea B2: Estratificación en split_luna por presencia de nódulos

**Archivos:**
- Modificar: `src/pipeline/fase0/pre_modelo.py` (función `split_luna`, líneas 740–803)

**Contexto:** LUNA tiene desbalance 10.7:1 (negativos/positivos a nivel de candidatos). Los demás datasets estratifican su split. `split_luna()` baraja aleatoriamente sin estratificar. La estratificación se hace a nivel de seriesuid: un UID es "positivo" si aparece en `annotations.csv` (nódulos reales), "negativo" si no.

**Cambio exacto — reemplazar el bloque de shuffle+split (líneas 764–774):**

```python
    # Estratificación por presencia de nódulos (positivo = UID en annotations.csv)
    annotations_path = luna_dir / "annotations.csv"
    if annotations_path.exists():
        try:
            ann = pd.read_csv(annotations_path)
            positive_uids = set(ann["seriesuid"].unique())
        except Exception:
            positive_uids = set()
        log.info(
            "[LUNA] %d UIDs positivos (con nódulos), %d negativos",
            len([u for u in all_uids if u in positive_uids]),
            len([u for u in all_uids if u not in positive_uids]),
        )
    else:
        positive_uids = set()
        log.warning("[LUNA] annotations.csv no encontrado — split sin estratificación")

    # Estratificar si hay suficientes positivos; fallback a shuffle aleatorio
    labels = np.array([1 if u in positive_uids else 0 for u in all_uids])
    n_positive = int(labels.sum())
    min_needed = 3  # mínimo para StratifiedShuffleSplit

    if n_positive >= min_needed and (len(all_uids) - n_positive) >= min_needed:
        from sklearn.model_selection import StratifiedShuffleSplit

        # Test split estratificado (10%)
        sss_test = StratifiedShuffleSplit(n_splits=1, test_size=0.10, random_state=SEED)
        trainval_idx, test_idx = next(sss_test.split(all_uids, labels))

        trainval_uids = [all_uids[i] for i in trainval_idx]
        trainval_labels = labels[trainval_idx]
        test_uids_list = [all_uids[i] for i in test_idx]

        # Val split estratificado (10% del total ≈ 11.1% de trainval)
        val_ratio = 0.10 / 0.90
        sss_val = StratifiedShuffleSplit(n_splits=1, test_size=val_ratio, random_state=SEED)
        train_idx_local, val_idx_local = next(sss_val.split(trainval_uids, trainval_labels))

        train_uids = [trainval_uids[i] for i in train_idx_local]
        val_uids = [trainval_uids[i] for i in val_idx_local]
        test_uids = test_uids_list
        log.info("[LUNA] Split estratificado por presencia de nódulos aplicado.")
    else:
        log.warning(
            "[LUNA] Insuficientes positivos (%d) para estratificar — split aleatorio.",
            n_positive,
        )
        rng = np.random.default_rng(SEED)
        uids_arr = np.array(all_uids)
        rng.shuffle(uids_arr)
        n_total = len(uids_arr)
        n_test = int(0.10 * n_total)
        n_val = int(0.10 * n_total)
        test_uids = uids_arr[:n_test].tolist()
        val_uids = uids_arr[n_test : n_test + n_val].tolist()
        train_uids = uids_arr[n_test + n_val :].tolist()
```

**Nota:** Importar `pd` ya existe en el módulo. `StratifiedShuffleSplit` se importa inline para no añadir dependencia en el módulo si no se necesita.

---

### Tarea B3: Eliminar duplicación de Páncreas en build_cae_splits

**Archivos:**
- Modificar: `src/pipeline/fase0/pre_modelo.py` (función `build_cae_splits`, sección Páncreas, líneas 1038–1064)

**Contexto:** `pancreas_splits.csv` tiene múltiples filas por `case_id` (una por fold en k-fold). El loop actual genera múltiples entradas por volumen, causando que el mismo archivo `.nii.gz` aparezca como `train` Y `val` en el CAE CSV (data leakage). Fix: usar solo el split del fold 1 (`fold1_train`, `fold1_val`, o `test`) para construir la contribución de Páncreas al CAE, y deduplicar por `ruta_imagen`.

**Cambio exacto — reemplazar el bloque Páncreas completo:**

```python
    # Pancreas (volúmenes .nii.gz con preprocesado on-the-fly)
    # IMPORTANTE: pancreas_splits.csv tiene múltiples filas por case_id (k-fold).
    # Se usa SOLO fold1 (split='fold1_train' | 'fold1_val' | 'test') para evitar
    # que el mismo volumen aparezca como train Y val en el CAE.
    pancreas_splits = datasets_dir / "pancreas_splits.csv"
    if pancreas_splits.exists():
        pdf = pd.read_csv(pancreas_splits)
        zenodo_dir = datasets_dir / "zenodo_13715870"

        # Seleccionar solo fold1 y test (un único rol por volumen)
        fold1_mask = pdf["split"].isin(["fold1_train", "fold1_val", "test"])
        pdf_canonical = pdf[fold1_mask].copy()

        # Normalizar split names
        pdf_canonical["norm_split"] = pdf_canonical["split"].map(
            {"fold1_train": "train", "fold1_val": "val", "test": "test"}
        )

        seen_paths: set = set()
        for _, prow in pdf_canonical.iterrows():
            norm_split = prow["norm_split"]
            nii_candidates = list(zenodo_dir.glob("{}*.nii.gz".format(prow["case_id"])))
            for nii in nii_candidates:
                nii_str = str(nii)
                if nii_str in seen_paths:
                    continue  # deduplicar por ruta
                seen_paths.add(nii_str)
                rows.append(
                    {
                        "ruta_imagen": nii_str,
                        "dataset_origen": "pancreas",
                        "split": norm_split,
                        "expert_id": 4,
                        "tipo_dato": "3d_volume_nifti",
                    }
                )
```

---

### Tarea B4: Guardia pre-CAE en run_splits

**Archivos:**
- Modificar: `src/pipeline/fase0/pre_modelo.py` (función `run_splits`, bloque CAE, líneas 1134–1139)

**Contexto:** `build_cae_splits()` se ejecuta siempre aunque algún split previo haya fallado, produciendo un CSV parcial sin warning explícito sobre la parcialidad.

**Cambio exacto — reemplazar el bloque CAE:**

```python
    # CAE — después de todos los splits individuales
    # Verificar que no hay errores bloqueantes antes de construir el manifiesto
    failed_splits = [
        k for k, v in results.items()
        if isinstance(v, dict) and v.get("status") == "error"
    ]
    if failed_splits:
        log.warning(
            "[CAE] Los siguientes splits fallaron: %s — "
            "cae_splits.csv se generará con datos parciales.",
            failed_splits,
        )
    try:
        results["cae"] = build_cae_splits(datasets_dir)
    except Exception as e:
        log.error("[CAE] Error construyendo cae_splits.csv: %s", e)
        results["cae"] = {"status": "error", "error": str(e)}
```

---

### Tarea B5: Documentar umbral 12% NIH en split_nih

**Archivos:**
- Modificar: `src/pipeline/fase0/pre_modelo.py` (líneas 88–92)

**Contexto:** El umbral del 12% y la tolerancia 1.05× no están documentados. Los números 88,999/11,349/11,772 son resultado de una ejecución, no una especificación fija.

**Cambio exacto — reemplazar el comentario de la línea 88:**

```python
    # Verificar si el test oficial excede 12% del total.
    # Umbral elegido: el test oficial NIH contiene ~12,000 imágenes de ~112,000 total
    # (~10.7%). Si en alguna ejecución el test > 12%, se reduce dinámicamente
    # moviendo pacientes sobrantes al pool train_val.
    # Referencia de splits esperados (ejecución canónica):
    #   train=88,999 | val=11,349 | test=11,772 (total=112,120)
    # La tolerancia 1.05× en la selección de pacientes de test evita cortes en
    # la mitad de un grupo de imágenes del mismo paciente.
    test_pct = len(te_imgs) / total
```

---

## Verificación final

Después de todos los cambios:

```bash
python -c "import ast; ast.parse(open('src/pipeline/fase0/fase0_pipeline.py').read()); print('fase0_pipeline OK')"
python -c "import ast; ast.parse(open('src/pipeline/fase0/pre_modelo.py').read()); print('pre_modelo OK')"
```

Verificar que no queda ninguna referencia a CvT-13 en fase0_pipeline.py:
```bash
grep -n "cvt13\|CvT-13\|CvT13" src/pipeline/fase0/fase0_pipeline.py
```

Verificar que `split_isic` ya no existe como nombre de función:
```bash
grep -n "def split_isic\|split_isic(" src/pipeline/fase0/pre_modelo.py
```

Verificar que `build_lesion_split` existe y se llama en run_splits:
```bash
grep -n "build_lesion_split" src/pipeline/fase0/pre_modelo.py
```

---

## Grupo C — Verificación de dataset_builder.py (solo lectura)

### Tarea C1: Verificar I-1 (NIH augmentación) e I-3 (leakage patient_id)

**Archivos a leer:**
- `src/pipeline/datasets/dataset_builder.py` (o equivalente en la ruta real)

**Qué verificar:**
- I-1: ¿`build_2d_aug_transform()` se importa y pasa como parámetro a `ChestXray14Dataset`?
- I-3: ¿`patient_ids_other` se pasa correctamente (no `None`) en la llamada a `ChestXray14Dataset`?

**Output:** Confirmar con grep exacto las líneas donde ocurren estos fixes.

---

## Registro de cambios

| ID | Archivo | Descripción | Estado |
|----|---------|-------------|--------|
| A1 | fase0_pipeline.py | paso7_cvt13 → paso7_densenet3d | ✅ 2026-04-13 |
| A2 | fase0_pipeline.py | SimpleITK en paso0_prerequisites | ✅ 2026-04-13 |
| B1 | pre_modelo.py | split_isic → build_lesion_split | ✅ 2026-04-13 |
| B2 | pre_modelo.py | Estratificación LUNA por nódulos | ✅ 2026-04-13 |
| B3 | pre_modelo.py | Dedup Páncreas en build_cae_splits | ✅ 2026-04-13 |
| B4 | pre_modelo.py | Guardia pre-CAE en run_splits | ✅ 2026-04-13 |
| B5 | pre_modelo.py | Documentar umbral 12% NIH | ✅ 2026-04-13 |
| C1 | dataset_builder.py | Verificar I-1 e I-3 (solo lectura) | ✅ 2026-04-13 — ambos confirmados |

### Resultado C1 — dataset_builder.py (verificación, sin cambios)

**I-1 (Augmentación NIH) — ✅ Implementado:**
- L27: `from transform_2d import build_2d_transform, build_2d_aug_transform`
- L85: `aug_transform_2d = build_2d_aug_transform(img_size=img_size)`
- L102: `aug_transform=aug_transform_2d` pasado solo al dataset de train
- En `chest.py` L324: aplicación condicional solo cuando `split=="train"`

**I-3 (Leakage patient_id) — ✅ Implementado:**
- L114: `patient_ids_other=chest_train.patient_ids` (val verifica contra train)
- L124: `patient_ids_other=chest_train.patient_ids | chest_val.patient_ids` (test verifica contra train∪val)
- En `chest.py` L154-165: cálculo y log de intersección de pacientes
